# 📊 System Architecture & Data Flow

## Overall System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│           FEEDBACK RESPONSE SYSTEM                          │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │          WEB INTERFACE (Frontend)                    │  │
│  │          frontend/index.html                         │  │
│  │                                                      │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  User Input Box (textarea)                   │   │  │
│  │  │  "Enter your feedback here..."               │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │                 ↓                                    │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  Analyze Button (JavaScript click)           │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │                 ↓                                    │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  Display Loading Spinner                     │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │                 ↓ (HTTP POST JSON)                   │  │
│  └──────────────────────────────────────────────────────┘  │
│                      ↓                                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │       API SERVER (Backend)                           │  │
│  │       frontend/api.py (Flask)                        │  │
│  │                                                      │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  Receive POST /predict                       │   │  │
│  │  │  {feedback: "user text"}                     │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │                 ↓                                    │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  Load Model & Vectorizer from Disk           │   │  │
│  │  │  - sentiment_model.pkl                       │   │  │
│  │  │  - vectorizer.pkl                            │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │                 ↓                                    │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  Vectorize Text using TF-IDF                 │   │  │
│  │  │  Transform string → sparse matrix (5000 dim) │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │                 ↓                                    │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  Run Model Inference                         │   │  │
│  │  │  LogisticRegression.predict(vectorized)      │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │                 ↓                                    │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  Get Sentiment & Confidence                  │   │  │
│  │  │  predict() → sentiment class                 │   │  │
│  │  │  predict_proba() → confidence score          │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │                 ↓                                    │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  Generate Personalized Response              │   │  │
│  │  │  responses[sentiment] → message              │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │                 ↓                                    │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  Return JSON Response                        │   │  │
│  │  │  {sentiment, response, confidence}           │   │  │
│  │  │  ↓ (HTTP 200 with JSON)                      │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                      ↓                                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │       FRONTEND DISPLAY                               │  │
│  │       index.html (JavaScript processing)             │  │
│  │                                                      │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  Receive JSON Response from API              │   │  │
│  │  │  Parse and extract data                      │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │                 ↓                                    │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  Update DOM Elements                         │  │  │
│  │  │  - Sentiment badge (with emoji & color)     │   │  │
│  │  │  - Response message                          │   │  │
│  │  │  - Confidence percentage                     │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │                 ↓                                    │  │
│  │  ┌──────────────────────────────────────────────┐   │  │
│  │  │  Display Results with Animation              │   │  │
│  │  │  - Slide-in effect                           │   │  │
│  │  │  - Color coding                              │   │  │
│  │  │  - Emoji indicators                          │   │  │
│  │  └──────────────────────────────────────────────┘   │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Data Processing Pipeline

```
INPUT TEXT
    ↓
┌─────────────────────────────────────────┐
│  Text Preprocessing (if applied)        │
│  - Lowercase conversion                 │
│  - Remove special characters            │
│  - Tokenization                         │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  TF-IDF Vectorization                   │
│  (vectorizer.pkl)                       │
│                                         │
│  Input:  String text                    │
│  Output: Sparse matrix (1, 5000)        │
│                                         │
│  TF-IDF Features:                       │
│  - Max 5000 features                    │
│  - Fitted on training data              │
│  - Normalized scores                    │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  Logistic Regression Model              │
│  (sentiment_model.pkl)                  │
│                                         │
│  Input:  Vector (1, 5000)               │
│  Output: Probability distribution       │
│                                         │
│  Classes: 6 sentiments                  │
│  [happiness, sadness, anger,            │
│   disgust, fear, surprise]              │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│  Decision Making                        │
│  - argmax(probabilities) → class        │
│  - max(probabilities) → confidence      │
└─────────────────────────────────────────┘
    ↓
OUTPUT
{
  sentiment: "happiness",
  confidence: 0.9467,
  response: "Thank you!...",
  feedback: "original text"
}
```

---

## Model Training Pipeline (Reference)

```
TRAINING PHASE (Already Done)
        ↓
┌─────────────────────────────┐
│  Load Training Data         │
│  - X_train.csv (feedback)   │
│  - y_train.csv (sentiment)  │
└─────────────────────────────┘
        ↓
┌─────────────────────────────┐
│  TF-IDF Vectorizer          │
│  - Fit on training data     │
│  - Learn vocabulary         │
│  - Create 5000 features     │
│  → vectorizer.pkl (saved)   │
└─────────────────────────────┘
        ↓
┌─────────────────────────────┐
│  Train Logistic Regression  │
│  - Max iterations: 1000     │
│  - Use vectorized X_train   │
│  - Learn sentiment patterns │
│  → sentiment_model.pkl      │
│     (saved)                 │
└─────────────────────────────┘
        ↓
┌─────────────────────────────┐
│  Evaluate Model             │
│  - Test on X_test, y_test   │
│  - Calculate accuracy       │
│  - Classification report    │
│  - Confidence/probability   │
└─────────────────────────────┘
```

---

## Sentiment Classification

```
┌─────────────────────────────────────────┐
│  SENTIMENT CATEGORIES (6 Classes)       │
├─────────────────────────────────────────┤
│                                         │
│  😊 HAPPINESS                           │
│     Positive sentiment, satisfaction    │
│     Examples:                           │
│     - "Love this product!"              │
│     - "Best purchase ever"              │
│                                         │
│  😢 SADNESS                             │
│     Negative, disappointed              │
│     Examples:                           │
│     - "Very disappointed"               │
│     - "Not what I expected"             │
│                                         │
│  😠 ANGER                               │
│     Angry, frustrated, critical         │
│     Examples:                           │
│     - "This is terrible!"               │
│     - "Worst experience ever"           │
│                                         │
│  🤢 DISGUST                             │
│     Repulsive, disgusted                │
│     Examples:                           │
│     - "This is awful"                   │
│     - "Completely disgusting"           │
│                                         │
│  😨 FEAR                                │
│     Concerned, worried, anxious         │
│     Examples:                           │
│     - "I'm worried about quality"       │
│     - "This scares me"                  │
│                                         │
│  😲 SURPRISE                            │
│     Unexpected, surprised               │
│     Examples:                           │
│     - "Didn't expect this!"             │
│     - "Surprising result"               │
│                                         │
└─────────────────────────────────────────┘
```

---

## File Relationships

```
Frontend (Web)
├── index.html
│   └── Sends HTTP requests to API
│       Displays responses
│
└── /api.py (Backend)
    ├── Loads sentiment_model.pkl
    ├── Loads vectorizer.pkl
    ├── Processes requests
    └── Returns predictions

Model Components
├── sentiment_model.pkl
│   ├── Type: Logistic Regression
│   ├── Input: Vector (1, 5000)
│   ├── Output: Sentiment class
│   └── Trained on: X_train, y_train
│
└── vectorizer.pkl
    ├── Type: TF-IDF Vectorizer
    ├── Input: Text string
    ├── Output: Sparse vector (5000 dims)
    └── Fitted on: X_train

Training Scripts
├── train_model.py
│   └── Creates model and vectorizer
│
├── preprocess.py
│   └── Cleans raw data
│
└── split_data.py
    └── Creates train/test split
```

---

## API Communication Protocol

```
FRONTEND (JavaScript)
    ↓
    ├─ Collects user feedback
    ├─ Creates JSON payload:
    │  {
    │    "feedback": "user's text"
    │  }
    │
    └─ Sends HTTP POST request
       Method: POST
       URL: http://localhost:5000/predict
       Headers: Content-Type: application/json
       Body: JSON above

BACKEND (Flask)
    ↓
    ├─ Receives request
    ├─ Extracts feedback text
    ├─ Vectorizes using TF-IDF
    ├─ Predicts sentiment
    ├─ Calculates confidence
    ├─ Gets response template
    │
    └─ Sends HTTP response
       Status: 200 OK
       Headers: Content-Type: application/json
       Body: {
         "sentiment": "happiness",
         "response": "Generated message",
         "confidence": 0.9467,
         "feedback": "original text"
       }

FRONTEND (JavaScript)
    ↓
    ├─ Receives response JSON
    ├─ Updates UI elements
    ├─ Displays sentiment badge
    ├─ Shows response text
    ├─ Displays confidence %
    │
    └─ User sees results!
```

---

## Error Handling Flow

```
USER INPUT
    ↓
[Frontend Validation]
    ├─ Empty text? → Show warning
    ├─ Special chars? → Accept
    └─ Valid? → Proceed
    ↓
[API Call]
    ├─ Network error? → Show connection error
    ├─ Server error? → Show server error
    ├─ Invalid response? → Show parse error
    └─ Success? → Display results
    ↓
[Backend Processing]
    ├─ Model not loaded? → Return 500
    ├─ Vectorizer error? → Return 500
    ├─ Prediction error? → Return 500
    └─ Success? → Return prediction
```

---

## Performance Considerations

```
Response Time Breakdown:
1. Frontend JS processing:      ~1-2ms
2. HTTP request creation:       ~0-1ms
3. Network latency:             ~10-50ms
4. Backend Flask processing:    ~5-10ms
5. Model loading (first call):  ~100-200ms
6. Vectorization:               ~5-10ms
7. Prediction:                  ~1-2ms
8. Response generation:         ~1-2ms
9. HTTP response:               ~0-1ms
10. Frontend display update:    ~10-20ms
                        ─────────────
TOTAL (first call):             ~150-300ms
TOTAL (cached model):           ~30-60ms
```

---

## Deployment Architecture (Optional)

```
Production Deployment:

┌──────────────────────────────────────────┐
│  Web Server (Nginx/Apache)               │
│  - Serves static files (HTML, CSS, JS)   │
│  - Proxies API requests to backend       │
└──────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│  Application Server (Gunicorn)           │
│  - Runs Flask application                │
│  - Handles multiple workers              │
│  - Scales with load                      │
└──────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│  Model Cache (Redis/Memcached)           │
│  - Caches model for faster access        │
│  - Reduces load time                     │
└──────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────┐
│  Database (if needed)                    │
│  - Store feedback history                │
│  - Track metrics                         │
└──────────────────────────────────────────┘
```

---

This document provides a visual representation of how all components work together!
